// import { v4 as uuid } from "uuid"

// function Key({ arr }) {
//     return (
//         <div>
//             {arr.map((el, i) =>
//                 // <li key={uuid()}> {el.name}</li>  //Give key whenever using the same element under the map function so that react can know which one is different
//             )}
//         </div>
//     )
// }
// export { Key }